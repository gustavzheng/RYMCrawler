[project]
name = "rym-crawler"
version = "0.1.0"
description = "Local RYM export enrichment with manual browser verification"
requires-python = ">=3.11"
dependencies = ["beautifulsoup4==4.15.0", "playwright==1.62.0", "html5lib==1.1"]

[tool.uv]
package = false
cache-dir = ".uv-cache"
